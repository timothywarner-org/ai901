# Lesson 8 -- Azure OpenAI chat via Microsoft Foundry (gpt-4o-mini, New Foundry)
#
# SETUP:
#   1. Run Deploy-Lesson08-Infrastructure.ps1 to provision the Foundry resource.
#   2. Copy this file to .env in the same folder.
#   3. Paste the values printed by the deploy script.
#   4. NEVER commit .env to source control.
#
# Get the endpoint:
#   az cognitiveservices account show \
#     -g rg-ai901-lesson08-demo -n YOUR-FOUNDRY-NAME \
#     --query properties.endpoint -o tsv
#
# Get the key:
#   az cognitiveservices account keys list \
#     -g rg-ai901-lesson08-demo -n YOUR-FOUNDRY-NAME \
#     --query key1 -o tsv
#
# AZURE_OPENAI_CHAT_DEPLOYMENT is the DEPLOYMENT name (not the model name).
# The deploy script names the deployment "gpt-4o-mini" to match the AI-901 exam
# answer; the underlying model is gpt-4.1-mini (the deployable successor).

AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE-NAME.cognitiveservices.azure.com/
AZURE_OPENAI_KEY=PASTE_KEY_HERE
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-10-21
